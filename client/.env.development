# Development-only client env vars (auto-loaded by Vite in dev mode)
# This points the OAuth redirect directly to Express, bypassing the Vite proxy,
# which is required for session cookies to persist during the Google OAuth flow.
VITE_API_URL=http://localhost:3000
